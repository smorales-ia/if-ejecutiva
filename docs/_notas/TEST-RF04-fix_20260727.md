# Test manual RF-04 · Fix de payload y submit

> **Fecha**: 27-jul-2026 · **Ejecuta**: Sergio (manual) · **Tandas cubiertas**: A · B · C
> **Objetivo**: probar que una solicitud creada desde IF-02 llega **completa** a
> Airtable. Hasta hoy no llegaba ninguna: el formulario nunca llamaba al backend.

---

## 0 · Precondiciones

Sin las tres primeras el test **no puede correr**. Verifícalas en orden.

| # | Precondición | Cómo se comprueba | Estado |
|---|---|---|---|
| 0.1 | **Blueprint SC01 importado en Make.com** | En el escenario: mód. 7 muestra `{{1.valor_uf}}` y `{{1.proyecto}}`; el filtro del Router 16 dice `contactos_visita existe` | ✅ confirmado 27-jul |
| 0.2 | **Escenario SC01 activo** (toggle ON) | Vista del escenario en Make | ☐ |
| 0.3 | **Deploy a Railway con el commit de esta sesión** | `/api/health` responde y el commit desplegado incluye `lib/mappers/crear-solicitud.ts` | ☐ |
| 0.4 | Copia stale `docs/make/SC01_-_Crear_solicitud_blueprint.json` eliminada | `ls docs/make/` | ☐ Sergio confirma |
| 0.5 | Variables en Railway: `MAKE_WEBHOOK_URL_SC01` · `MAKE_HMAC_SECRET` · `AIRTABLE_TOKEN` · `AIRTABLE_BASE_ID` | Panel de Railway | ☐ |

> **0.5 no es opcional para el 409.** El pre-chequeo de duplicados lee Airtable
> directamente. Si falta `AIRTABLE_TOKEN`, el chequeo devuelve `indeterminado`,
> el alta continúa sin validar y se registra un `console.warn` — el caso 4 del
> test daría un falso "pasa".

---

## 1 · Caso principal · alta manual completa

### 1.1 · Datos de entrada

Modo **Manual**, propiedad **Nueva** (activa el bloque financiero y la rama
"jurídica" del vendedor — es la ruta que ejercita más campos).

Usa un **N° de operación que no exista** en `TX_Solicitudes`. Anótalo:

```
N° de operación usado en el test: ______________
```

| Sección | Campo | Valor sugerido |
|---|---|---|
| **A** | Canal de origen | *(cualquiera del select)* |
| A | Cliente | *(cualquiera)* |
| A | Tipo de cliente de origen | *(cualquiera)* |
| A | Tipo de informe | *(el que ofrezca el cliente elegido)* |
| A | Banco (originador) | `BancoEstado` |
| A | N° de operación cliente | *el anotado arriba* |
| A | Sucursal originadora | `Providencia` |
| A | Ejecutivo solicitante | `Test Ejecutivo` |
| A | Ejec. Formalizador | `Test Formalizador` |
| A | Ejec. Comercializador | `Test Comercializador` ← **se descarta a propósito**, ver §4 |
| **B** | Proyecto o condominio | `Edificio Test RF04` |
| B | Dirección | `Av. Apoquindo 5230` |
| B | Origen dirección · Región · Comuna · Tipo de propiedad · Estado de conservación | *(cualquiera)* |
| B | Razón social (vendedor) | `Inmobiliaria Test SpA` |
| B | RUT inmobiliaria | `76.123.456-7` |
| B | Correo / Teléfono / Origen del dato del vendedor | *(cualquiera válido)* |
| B | Unidades | 1 unidad completa con respaldo ← **se descarta a propósito**, ver §4 |
| **C** | Comprador: nombre · RUT · email · teléfono | RUT válido con DV real |
| C | Contactos de visita | **2 contactos**, ambos con teléfono, roles distintos |
| **D** | Producto | `hipotecario` (fuerza banco financista obligatorio) |
| D | Banco financista | `Banco Santander` |
| D | Observaciones | `Test RF-04 fix payload 27-jul` |
| D | Valor total UF | `4.200` ← **escríbelo con punto de miles a propósito** |
| D | Subsidio · Ahorro · Mutuo · Pago contado · Bonos · Precio de venta | valores distintos entre sí, para detectar cruces |

> **El `4.200` no es capricho.** Prueba que `numeroPlano()` normaliza el formato
> es-CL antes de enviar. Si en Airtable aparece **4** en vez de **4200**, la
> normalización no se aplicó y `parseNumber()` de Make cortó en el punto.

### 1.2 · Comportamiento esperado en pantalla

| ☐ | Al pulsar "Crear solicitud" el botón se deshabilita y muestra spinner + "Creando…" |
| ☐ | El sheet **no** se puede cerrar mientras dice "Creando…" |
| ☐ | Aparece toast **verde**: `Solicitud creada · SOL-…` (con el `codigo_ext` real) |
| ☐ | El formulario se limpia y el sheet se cierra solo |

Anota el código devuelto:

```
codigo_ext devuelto por el toast: ______________
```

> Si el toast dice **"Solicitud creada"** sin código, la solicitud **sí** se
> creó pero SC01 no devolvió `codigo_ext` — revisa el módulo 8 (Webhook
> Response) del escenario. No es un fallo del alta.

---

## 2 · Verificación en Airtable

### 2.1 · `TX_Solicitudes` (`tblaHTyMHYfmy7Fg6`)

Abre el registro nuevo. **42 campos deben estar poblados.** Los que traen ⚠ son
los que estaban rotos antes de este fix — si alguno sigue vacío, el fix no llegó.

**Cabecera y automáticos**

| ☐ | Campo | Esperado |
|---|---|---|
| ☐ | `estado` | `creada` |
| ☐ | `codigo_ext` | igual al del toast |
| ☐ | `origen_canal` | `ingreso_manual` |
| ☐ | `fecha_solicitud` | hoy |
| ☐ | `ejecutiva_asignada` | ⚠ **tu usuario** — resuelto por el Search del mód. 15 vía `ejecutiva_clerk_id`. Estaba roto: el interface no declaraba la clave |

**Sección A**

| ☐ | Campo | Esperado |
|---|---|---|
| ☐ | `canal_contacto_original` | el canal elegido |
| ☐ | `cliente` | link al cliente |
| ☐ | `tipo_cliente_origen` | ⚠ el tipo elegido |
| ☐ | `tipo_informe` | ⚠ link — el Search usaba `tipoInforme` |
| ☐ | `banco` | `banco_estado` (slug, **no** "BancoEstado") |
| ☐ | `n_operacion_cliente` | el número anotado |
| ☐ | `sucursal_originadora ` | `Providencia` *(ojo: el campo tiene espacio final en Airtable, D-08)* |
| ☐ | `ejecutivo_solicitante` | `Test Ejecutivo` |
| ☐ | `ejecutivo_formalizador` | ⚠ `Test Formalizador` |

**Sección B**

| ☐ | Campo | Esperado |
|---|---|---|
| ☐ | `tipo_propiedad_nuevo_usado` | ⚠ `nuevo` |
| ☐ | `proyecto_condominio` | ⚠ `Edificio Test RF04` — **mapping nuevo (A5)**, antes no existía |
| ☐ | `direccion` | `Av. Apoquindo 5230` |
| ☐ | `origen_direccion` | ⚠ el elegido |
| ☐ | `region` | la elegida |
| ☐ | `comuna` | link |
| ☐ | `tipo_propiedad` | ⚠ link — el Search usaba `tipoPropiedad` |
| ☐ | `estado_conservacion` | ⚠ el elegido |
| ☐ | `vendedor_tipo_persona` | ⚠ `juridica` — **derivado**, antes nunca se enviaba |
| ☐ | `vendedor_razon_social_o_nombre` | ⚠ `Inmobiliaria Test SpA` |
| ☐ | `vendedor_rut` | ⚠ `76.123.456-7` (el RUT de la **inmobiliaria**, no el del comprador) |
| ☐ | `vendedor_email` · `vendedor_telefono` · `vendedor_origen_dato` | ⚠ los capturados |

**Sección C** — claves cortas sin prefijo = siempre el comprador

| ☐ | Campo | Esperado |
|---|---|---|
| ☐ | `cliente_final_nombre` | ⚠ nombre del comprador |
| ☐ | `cliente_final_rut` | ⚠ RUT del comprador |
| ☐ | `email_contacto` | ⚠ email del comprador |
| ☐ | `solicitante_telefono` | ⚠ teléfono del comprador |

**Sección D**

| ☐ | Campo | Esperado |
|---|---|---|
| ☐ | `producto` | link |
| ☐ | `banco_financista` | ⚠ link a `Banco Santander` |
| ☐ | `observaciones_internas` | el texto |
| ☐ | `financiero_valor_total_uf` | ⚠ **4200** (no 4) |
| ☐ | `monto_estimado_uf` | ⚠ **4200** — alimentado por `valor_uf` (decisión hallazgo 3) |
| ☐ | `financiero_subsidio_uf` · `_ahorro_uf` · `_mutuo_uf` · `_pago_contado_uf` · `_bono_captacion_uf` · `_bono_integracion_uf` · `_precio_venta_uf` | ⚠ cada uno con **su** valor, sin cruces |

**Deben quedar vacíos** (correcto, no es un fallo):

| ☐ | Campo | Por qué |
|---|---|---|
| ☐ | `modo_creacion` | el wizard lo decide pero no está en el zod |
| ☐ | `email_thread_id` · `correo_cliente_ref` | sin captura en UI |
| ☐ | `fecha_asignacion_ts` | correcto en un alta |
| ☐ | `TX_Unidades` (link) | SC01 no tiene módulo de unidades — fase 2 |

### 2.2 · `TX_ContactosVisita` (`tblW3SSbKo6vRjwBJ`)

| ☐ | Comprobación |
|---|---|
| ☐ | Hay **exactamente 2 filas nuevas** (una por contacto) |
| ☐ | Ambas enlazan a la solicitud recién creada |
| ☐ | `nombre` · `telefono` · `email` · `rol` correctos en cada una |
| ☐ | ⚠ `estado_contacto` poblado — el zod lo llama `estado`, el mapper lo renombra |
| ☐ | ⚠ `orden_prioridad` = **1** en el primero y **2** en el segundo — generado desde el índice, el formulario no lo captura |

> **Si hay 0 filas**, el filtro del Router 16 no encontró `contactos_visita`:
> el blueprint importado no es el actualizado. Es una falla silenciosa — Make
> **no** reporta error, simplemente no ejecuta la rama.

### 2.3 · `LogEscenarios` (`tblR4VWpUHw1CSyIS`) — la verificación más útil

`postToMake` escribe aquí en **cada** llamada. La fila nueva contiene el payload
literal que se envió.

| ☐ | Comprobación |
|---|---|
| ☐ | Fila nueva con `escenario = SC01` y `estado = ok` |
| ☐ | En `payload_enviado`: **todas las claves en snake_case**, ninguna en camelCase |
| ☐ | Existe `contactos_visita` (con guion bajo), **no** `contactosVisita` |
| ☐ | Existe `valor_uf` y vale `4200` |
| ☐ | Existe `proyecto` |
| ☐ | Existe `ejecutiva_clerk_id` con tu `user_…` de Clerk |
| ☐ | **No** aparecen `unidades`, `ejec_comercializador` ni `vendedorCoincideComprador` |

> Si el payload es correcto aquí pero Airtable quedó incompleto, el problema
> está en Make, no en la app. Esa separación es el motivo de revisar este paso
> antes que el historial de ejecuciones.

### 2.4 · `Z_ColaPendientes` (`tblSvEtzO2TdmkBfk`)

| ☐ | Comprobación |
|---|---|
| ☐ | **Sin filas nuevas** |

> **Nota de alcance.** Verifiqué el blueprint: **SC01 no tiene ningún módulo
> que escriba en `Z_ColaPendientes`** — sólo escribe en `TX_Solicitudes` y
> `TX_ContactosVisita`. La tabla aparece en el JSON únicamente como *label* de
> un link field en la metadata del conector. O sea: este check pasa por
> construcción y no prueba nada sobre SC01. Se mantiene porque **si aparece una
> fila, vino de otro escenario** y eso sí es información. La superficie real de
> errores de SC01 es `LogEscenarios` (§2.3) y el historial de ejecuciones de
> Make.

### 2.5 · Historial de ejecuciones en Make

| ☐ | Comprobación |
|---|---|
| ☐ | 1 ejecución con estado **Success** |
| ☐ | Los 7 módulos `Search` devolvieron 1 bundle cada uno (ninguno vacío) |
| ☐ | El Iterator 17 procesó 2 bundles |

> Un `Search` con 0 bundles significa que el nombre enviado no existe en la
> tabla maestra. El módulo 7 recibiría un link vacío sin fallar.

### 2.6 · AT01 (automation de Airtable)

| ☐ | Comprobación |
|---|---|
| ☐ | `regla_aplicada` se pobla en < 10 s (criterio de `docs/construccion.md`) |

---

## 3 · Casos de error

### 3.1 · N° de operación duplicado → 409

Crea **otra** solicitud repitiendo el mismo N° de operación del caso 1.

| ☐ | Comprobación |
|---|---|
| ☐ | El sheet **no** se cierra |
| ☐ | El campo "N° de operación" queda marcado en rojo con `Ya existe una solicitud con el N° de operación {n}.` |
| ☐ | La alerta destructiva del **inicio del formulario** lista el mismo error (doble superficie §1.5.1) |
| ☐ | La vista hace scroll hasta esa alerta |
| ☐ | Toast rojo, con `Corresponde a {codigo_ext}` si el backend lo devolvió |
| ☐ | **No** se creó una segunda fila en `TX_Solicitudes` |
| ☐ | **No** hay fila nueva en `LogEscenarios` — el 409 corta antes de llamar a Make |
| ☐ | Al corregir el número, el error desaparece solo (revalidación `onChange`) |

### 3.2 · Error de Make → toast rojo genérico

Desactiva el escenario SC01 en Make e intenta crear.

| ☐ | Comprobación |
|---|---|
| ☐ | Toast rojo: `No pudimos completar la acción. Intenta nuevamente en unos segundos.` |
| ☐ | El sheet queda abierto **con todo lo escrito intacto** |
| ☐ | Ningún mensaje técnico (status HTTP, stack, nombre de módulo) llega al usuario |

**Reactiva SC01 al terminar.**

### 3.3 · Validación de forma (regresión)

Envía el formulario con un RUT inválido.

| ☐ | Comprobación |
|---|---|
| ☐ | Mensaje literal §6: `Necesitamos el RUT del comprador con su dígito verificador. Ej.: 12.345.678-9.` |
| ☐ | No sale ninguna petición al backend (pestaña Network vacía) |

---

## 4 · Lo que este test **no** cubre (por diseño)

No son fallos. Están fuera del alcance de esta sesión y entran en RF-04 fase 2:

| Qué | Por qué |
|---|---|
| **Bloque Unidades completo** | SC01 no tiene módulo que cree unidades. Los 16 subcampos viajan en el formulario y se descartan en el mapper |
| `ejec_comercializador` | No existe el campo en `TX_Solicitudes` (deuda de schema) |
| `vendedorCoincideComprador` | Sin campo destino |
| `vendedor_nombre` (`fldfUXb9vzxklu8ES`) | Queda vacío: SC01 escribe sólo `vendedor_razon_social_o_nombre` |
| Familia `fin_*_uf` | 8 campos huérfanos en paralelo a `financiero_*_uf` |
| Adjuntos | Fase posterior, vía `/api/adjuntos/upload` |
| Rama **Usado** | El caso 1 prueba **Nuevo**. Conviene repetirlo en Usado para validar que `vendedor_tipo_persona = natural` y que el par nombre/RUT toma `vendedorNombre`/`vendedorRut` |

---

## 5 · Resultado

```
Fecha de ejecución : ______________
Caso 1 (alta)      : ☐ pasa   ☐ falla → ____________________________
Caso 3.1 (409)     : ☐ pasa   ☐ falla → ____________________________
Caso 3.2 (Make)    : ☐ pasa   ☐ falla → ____________________________
Caso 3.3 (forma)   : ☐ pasa   ☐ falla → ____________________________
codigo_ext creado  : ______________
```

Si algo falla, el orden de diagnóstico es: **`LogEscenarios` §2.3 primero**
(¿salió bien el payload?) → historial de Make §2.5 (¿falló un módulo?) →
mapping del módulo 7 (¿apunta a la clave correcta?).
