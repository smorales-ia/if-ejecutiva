# Snapshot · 12-ago-2026 15:00 — Manual de pruebas v2 (PDF premium)

## Tarea

Generar `docs/_md/Manual_Usuario_Prueba1_v2.pdf`: versión premium del manual, reorientada a
**guía de pruebas** para que la Ejecutiva Comercial (usuario no técnico) valide la UI de
IF-02 · Consola Ejecutiva (CU-002).

## Insumos

- `docs/_md/VProperty_Especificacion_Proyecto_v1_9_8.md` — §1.0 a §1.6 (fuente de RF, RN y
  criterios de aceptación).
- `docs/_md/Manual_Usuario_Prueba1.pdf` — 3 páginas, 3 imágenes (capturas de pantalla), sin
  texto extraíble (Word → PDF, todo imagen). Se preserva intacto (RG-09).
- Código real de `components/console/*` — para que los literales de la guía coincidan con
  la UI desplegada, no sólo con el spec.

## Entorno

- El sistema no traía `pip`. Se instaló en modo usuario: `python3 get-pip.py --user
  --break-system-packages` (Python 3.14.4, PEP 668 activo). Luego
  `pip install --user --break-system-packages pymupdf reportlab`.
- PyMuPDF 1.28.2 · reportlab 5.0.0 · fuente embebida DejaVu Sans (`/usr/share/fonts/truetype/dejavu`).

## Plan

1. Extraer imágenes con PyMuPDF → `docs/_artefactos/manual_imgs/img_001..003.png`. ✅
2. Leer §1 del spec + literales reales de la UI. ✅
3. Diseñar plantilla premium (portada, índice enlazado, header/footer, callouts, badges).
4. Escribir 15 casos de prueba con los 5 bloques fijos de RG-03.
5. Generar el PDF con reportlab y verificar el checklist.
6. Snapshot final.

## Hallazgos previos a la redacción

- **No existe control de "Cerrar sesión"** en la app: `components/console/app-header.tsx:41`
  pinta un `Avatar` con iniciales fijas "ME", sin `UserButton` de Clerk ni menú. El caso de
  prueba de cierre de sesión se redacta contra esa realidad (verificar protección de acceso)
  y se marca como limitación conocida.
- **Vista "Mi cartera" puede degradarse**: `solicitud-list.tsx:404` muestra "La vista Mi
  cartera aún no está disponible" cuando no se resuelve la ejecutiva en `AUTH_Usuarios`.
- **CI-002** (disparo del webhook de extracción) explica dos limitaciones visibles para la
  Ejecutiva: el modo "En base a documentos adjuntos" de la Fase 1 está deshabilitado
  (spec §1.5.0, v1.9.8) y los documentos subidos quedan sin datos extraídos.

## Punto de reanudación

Imágenes ya extraídas y spec leído. Si la sesión se corta aquí, retomar en el paso 4:
escribir el generador reportlab y componer el PDF.
