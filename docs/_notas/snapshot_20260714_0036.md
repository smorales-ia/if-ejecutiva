# Snapshot de continuidad — 2026-07-14 00:36

> Snapshot reconstruido manualmente desde Claude (interfaz web) porque la sesión de Claude Code se cortó por límite de tokens antes de ejecutar la rutina de snapshot.

---

## 1. Contexto

- **RF en curso**: RF-09 — Extracción documental con Claude API.
- **Fase**: Reconstruir estado del proyecto RF-09 desde archivos.
- **Tanda activa**: **Tanda D** (implementación completa de RF-09 en Make + Airtable + Next.js).
- **Checkpoint pendiente**: **Checkpoint 1** — Claude Code se quedó esperando confirmación de Sergio con 3 puntos verificados antes de continuar.
- **Decisiones firmadas relevantes**: A, B, C, D, E (las inherited del diseño RF-09 en `docs/_notas/rf09_diseno.md`).
- **Último aprendizaje aplicable**: revisar tope de `docs/aprendizajes.md` para la última entrada E-XXX antes de continuar.

---

## 2. Estado actual

### Hecho
- Diseño RF-09 documentado en `docs/_notas/rf09_diseno.md`.
- Campos de dominio creados vía MCP: `D_Atributo.version` y `D_Documento.extraccion_incompleta`.
- Blueprint SC-Adjuntos-Upload funcional (RF-44 a RF-50 cerrados en Fase Adjuntos 1).
- Airtable MCP conectado en Claude Code (`.mcp.json`, scope proyecto, WSL2).

### A medias
- **Tanda D de RF-09**: Claude Code detuvo su respuesta en "listo checkpoint 1" esperando que Sergio verifique y reporte 3 puntos:
  1. **Input vars vs Secrets** en Make: si la UI de Make expone Secrets o no, para decidir cómo pasar `MAKE_WEBHOOK_URL_RF09` y `MAKE_RF09_HMAC_SECRET`. Si no expone Secrets, dejar en OFF + dejar nota del 2º trigger de reintento.
  2. **Nombre real del módulo Dropbox** en el escenario RF-09 (verificar que sea `dropbox:uploadLargeFile` v5, no `dropbox:uploadFile` v1).
  3. **Limpieza regex del JSON de Claude** en el módulo ParseJSON 11 (verificar el patrón de limpieza antes del parseo).

### No empezado (esperando checkpoint 1)
- **Tarea 3 — AT03-Ext**: trigger When record updated watching solo `atributos_obtenidos`, pegar script, input `adjuntoId`, más bloque de verificación de dependencias (3 campos que ya se verificó que existen, y por qué la propagación puede legítimamente hacer skip).
- **Tarea 4 — Importar blueprint en Make**: exportar E3 primero para reciclar el module-id de Dropbox, resolver las 2 conexiones placeholder, verificación módulo por módulo:
  - Webhook → copiar URL a Airtable
  - HMAC
  - Módulos Airtable campo por campo
  - Dropbox módulo 9
  - HTTP → Claude módulo 10
  - ParseJSON módulo 11
  - Router
  - LogEscenarios con nombres reales
  - `ANTHROPIC_API_KEY` como variable del escenario
- **Tarea 5 — Prueba end-to-end**: preparar adjunto idle, test de cada automation en orden, definir qué fila esperar en `LogEscenarios` / `D_Documento` / `D_DocumentoValorAtributo` (RN-32: una sola columna de valor), y activar solo tras ciclo exitoso.

---

## 3. Próxima acción atómica

Sergio verifica los 3 puntos del checkpoint 1 y responde a Claude Code con:

```
listo checkpoint 1

1. Input vars vs Secrets: [ELEGIR: "Make expone Secrets, usar Secrets" | "Make NO expone Secrets, usar Input vars con OFF + nota 2º trigger reintento"]
2. Nombre real módulo Dropbox: [PEGAR NOMBRE EXACTO desde escenario E3 exportado]
3. Regex limpieza JSON Claude: [PEGAR REGEX FINAL ajustado]

Procede con Tareas 3, 4 y 5 en orden.
```

---

## 4. Tareas manuales pendientes de Sergio

### 4.1 Verificar UI de Make (Input vars vs Secrets)
1. Abrir el escenario RF-09 en Make (org 7487039, EU1).
2. Ir a "Scenario settings" → buscar sección "Variables".
3. Confirmar si aparece la opción "Secrets" separada de "Input variables".
4. Anotar el resultado para reportar en checkpoint 1.

### 4.2 Verificar nombre real del módulo Dropbox
1. En Make, abrir un escenario existente que suba archivos a Dropbox (SC-Adjuntos-Upload).
2. Click en el módulo Dropbox → ver el nombre técnico (debe ser `dropbox:uploadLargeFile` v5).
3. Copiar ese nombre exacto para reportar en checkpoint 1.

### 4.3 Verificar Railway env vars
Confirmar que existen en Railway (dashboard → Variables):
- `MAKE_WEBHOOK_URL_RF09`
- `MAKE_RF09_HMAC_SECRET`
- `ANTHROPIC_API_KEY`

### 4.4 Verificar Airtable (solo lectura, ya hecho antes pero re-confirmar)
- `TX_Adjuntos.estado_extraccion` — singleSelect existe.
- `D_Atributo.version` — existe.
- `D_Documento.extraccion_incompleta` — existe.

### 4.5 Decisión de capacidad Make (POSTERGADA)
- Make Free plan permite **solo 2 escenarios activos**.
- RF-09 requiere un **3º escenario activo**.
- **Decisión de Sergio (2026-07-14)**: postergar hasta justo antes de Tarea 5.
- Tareas 3 y 4 pueden ejecutarse completas sin activar el escenario.
- Reabrir esta decisión antes de Tarea 5 con opciones:
  - Opción A: Upgrade a Make Core plan.
  - Opción B: Rotación de escenarios (desactivar uno temporalmente).

---

## 5. Riesgos y decisiones abiertas

- ⚠️ **Make plan capacity** (bloqueador duro): sin decisión de Sergio no se puede activar el escenario RF-09.
- ⚠️ **Router vs Airtable Script para EAV (RN-32)**: decisión inherited pendiente. Verificar antes de escribir código.
- ⚠️ **Primary field real de `D_TipoDocumento`**: confirmar vía MCP antes de escribir lookup formulas.
- ⚠️ **Regex limpieza JSON Claude**: depende de muestras reales de output del modelo. Riesgo de romper el parseo si el patrón no cubre casos edge.
- ⚠️ **Módulo Dropbox correcto**: si se importa blueprint con `dropbox:uploadFile` v1 en lugar de `dropbox:uploadLargeFile` v5, la subida falla en archivos > 150 MB.

---

## 6. Cómo retomar (prompt literal para próxima sesión Claude Code)

Cuando Sergio abra una nueva sesión de Claude Code, pegar exactamente este prompt de arranque:

```
Sesión previa de RF-09 Tanda D se cortó por límite de tokens antes del checkpoint 1.

Antes de cualquier acción:
1. Lee, en este orden:
   - docs/_notas/snapshot_20260714_0036.md
   - CLAUDE.md
   - docs/_notas/rf09_diseno.md
   - docs/aprendizajes.md (últimas 20 entradas)
   - docs/construccion.md (sección RF-09)

2. Confirma que entiendes el estado en el que quedó Tanda D según el snapshot.

3. NO ejecutes nada aún. Responde solo con:
   - "Snapshot leído."
   - Un resumen de 5 líneas de dónde quedamos.
   - Los 3 puntos que debo verificar del checkpoint 1.
   - Espera mi respuesta con "listo checkpoint 1" antes de seguir con Tareas 3, 4 y 5.

Responde como panel de expertos (Enterprise Architect + Integrations Engineer + QA Lead).
Todo en español. Guarda un nuevo snapshot cada vez que cierres un hito.
```

---

## Notas de este snapshot

- Reconstruido manualmente desde Claude (interfaz web) porque Claude Code perdió la ventana de ejecución.
- Fuentes: pantalla visible al momento del corte + memorias del proyecto VProperty + `docs/_notas/rf09_diseno.md`.
- Si algún dato de la Tanda D quedó fuera (submódulos internos del blueprint que no aparecían en pantalla), Claude Code deberá inspeccionar el escenario en Make antes de proceder con Tarea 4.
