# Snapshot — Fix filtro contactos por record id (SC-Edicion) — 2026-07-24 17:10 (-04)

> Base `app9G7lLkIV3CpeLa`. Continúa `snapshot_20260724_1704.md`. Sin commit/push.
> Corrige el caveat del filtro `{solicitud} = '...'` (comparaba por primary field, no por record id).

## Campos creados vía MCP

| Tabla | Campo | Tipo | FIELD_ID |
|---|---|---|---|
| TX_Solicitudes (tblaHTyMHYfmy7Fg6) | `record_id` | formula `RECORD_ID()` | **fldx3ewhqGRv99uwZ** |
| TX_ContactosVisita (tblW3SSbKo6vRjwBJ) | `solicitud_record_id` | lookup (link `solicitud` → `record_id`) | **fldYNKk5cyfWLxwqD** |

Lookup config: `recordLinkFieldId=fldSQAKu5ooRgF5uw` (solicitud) · `fieldIdInLinkedTable=fldx3ewhqGRv99uwZ` (record_id). `isValid:true`, result singleLineText.

## Cambio en el blueprint

Archivo: `docs/make/modificado/SC-Edicion_-_Edicion_de_solicitud_blueprint.json`
Módulo `id=13` (`airtable:ActionSearchRecords`, tabla `tblW3SSbKo6vRjwBJ`):

- Antes: `{solicitud} = '{{1.solicitudId}}'`
- Ahora: `{solicitud_record_id} = "{{1.solicitudId}}"`

Nada más se tocó (mapeos, IDs de módulos, `hook`, `__IMTCONN__` 8847431 intactos). JSON validado.

## Docs actualizados
- `docs/make/field_ids_if02.json`: +`record_id` (extra TX_Solicitudes), +`solicitud_record_id` (TX_ContactosVisita), +nota en `_meta`.
- `docs/construccion.md`: sección "Fix filtro contactos por record id (24-jul-2026)" y caveat obsoleto removido.

## Confirmación
Filtro compara por record id real.
