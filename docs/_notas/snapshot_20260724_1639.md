# Snapshot schema IF-02 v1.9 — 2026-07-24 16:39 (-04)

> Sesión "Airtable Engineer + Data Designer". Fuente: instrucción de sesión basada en
> `VProperty_Especificacion_Proyecto_v1_9_1.md §1.5.1`. Base `app9G7lLkIV3CpeLa`.
> Método: `list_tables_for_base` + `get_table_schema` (dominios de singleSelect, targets de link,
> precisión numérica, tipos date/dateTime). Regla dura respetada: no re-crear lo existente;
> no modificar singleSelect con opciones distintas — reportar.

## Hallazgo global

El schema v1.9 que `schema-airtable.md §20` marcaba como *pendiente* **ya estaba creado** en la base
(los 8 registros semilla de `M_TiposDeBien` tienen `createdTime` 2026-07-24T15:41Z). La única acción de
creación de esta sesión fueron los **9 campos legales de `TX_DocumentosLegales`**. `TX_Vendedor` NO se
creó: el vendedor vive como campos `vendedor_*` en `TX_Solicitudes` (decisión de equipo, relación 1:1).

## Tabla-resumen {tabla, campo, acción}

### TX_ContactosVisita (tblW3SSbKo6vRjwBJ)
| tabla | campo | acción |
|---|---|---|
| TX_ContactosVisita | (tabla) | existía_ok |
| TX_ContactosVisita | nombre | existía_ok |
| TX_ContactosVisita | telefono | existía_ok |
| TX_ContactosVisita | email | existía_ok |
| TX_ContactosVisita | rol (propietario·corredor·arrendatario·conserje·otro) | existía_ok |
| TX_ContactosVisita | orden_prioridad | existía_ok |
| TX_ContactosVisita | estado_contacto (valido·no_contesta·telefono_erroneo) | existía_ok |
| TX_ContactosVisita | solicitud → TX_Solicitudes | existía_ok |

### M_TiposDeBien (tblQkurIaEqg6tMA4)
| tabla | campo | acción |
|---|---|---|
| M_TiposDeBien | (tabla) | existía_ok |
| M_TiposDeBien | nombre (primary) | existía_ok |
| M_TiposDeBien | codigo | existía_ok |
| M_TiposDeBien | activo (checkbox) | existía_ok |
| M_TiposDeBien | 8 semillas (Edificación…Obras complementarias) | existía_ok |

### TX_Solicitudes (tblaHTyMHYfmy7Fg6)
| tabla | campo | acción |
|---|---|---|
| TX_Solicitudes | ejecutivo_formalizador | existía_ok |
| TX_Solicitudes | tipo_propiedad | **conflicto** (Link→M_TiposPropiedad, no singleSelect nuevo/usado) |
| TX_Solicitudes | modo_creacion (documentos·manual) | existía_ok |
| TX_Solicitudes | tipo_cliente_origen (correo_texto·correo_ficha·extranet) | existía_ok |
| TX_Solicitudes | email_thread_id | existía_ok |
| TX_Solicitudes | correo_cliente_ref | existía_ok |
| TX_Solicitudes | estado_conservacion (nuevo·sin_uso·bueno·normal·malo·deficiente) | existía_ok |
| TX_Solicitudes | origen_direccion | **conflicto** (opciones cert_avaluo·cert_numero ≠ certificado_*) |
| TX_Solicitudes | fecha_asignacion | **conflicto** (date, no dateTime) |
| TX_Solicitudes | vendedor_tipo_persona (juridica·natural) | existía_ok |
| TX_Solicitudes | vendedor_razon_social_o_nombre | existía_ok |
| TX_Solicitudes | vendedor_rut | existía_ok |
| TX_Solicitudes | vendedor_email | existía_ok |
| TX_Solicitudes | vendedor_telefono | existía_ok |
| TX_Solicitudes | vendedor_origen_dato | **conflicto** (opción cert_avaluo ≠ certificado_avaluo) |
| TX_Solicitudes | financiero_valor_total_uf (number,2) | existía_ok |
| TX_Solicitudes | financiero_subsidio_uf (number,2) | existía_ok |
| TX_Solicitudes | financiero_ahorro_uf (number,2) | existía_ok |
| TX_Solicitudes | financiero_mutuo_uf (number,2) | existía_ok |
| TX_Solicitudes | financiero_pago_contado_uf (number,2) | existía_ok |
| TX_Solicitudes | financiero_bono_captacion_uf (number,2) | existía_ok |
| TX_Solicitudes | financiero_bono_integracion_uf (number,2) | existía_ok |
| TX_Solicitudes | financiero_precio_venta_uf (number,2) | existía_ok |

### TX_Unidades (tbl2QDLvJDyy3Rg2I)
| tabla | campo | acción |
|---|---|---|
| TX_Unidades | modelo | existía_ok |
| TX_Unidades | superficie_terraza_m2 (number,2) | existía_ok |
| TX_Unidades | superficie_terreno_m2 | **conflicto** (existe como sup_terreno_m2, number precision 0) |
| TX_Unidades | con_rol_o_uso_y_goce (con_rol·uso_y_goce) | existía_ok |
| TX_Unidades | rol_sii_en_tramite (checkbox) | existía_ok |
| TX_Unidades | ampliacion_m2 (number,2) | existía_ok |
| TX_Unidades | ampliacion_regularizable (si·no·no_aplica) | existía_ok |
| TX_Unidades | origen_superficie (carta_ficha_inmobiliaria·plano·base_interna_sii·certificado_avaluo·medicion_tasador) | existía_ok |
| TX_Unidades | respaldo_adjunto → TX_Adjuntos | existía_ok |
| TX_Unidades | detalle_item (multiline) | existía_ok |
| TX_Unidades | tipo_bien → M_TiposDeBien | existía_ok |

### TX_DocumentosLegales (tbl7qIg5x4Y0tOiLk)
| tabla | campo | acción | FIELD_ID |
|---|---|---|---|
| TX_DocumentosLegales | (tabla + link solicitud) | existía_ok | fldJ60CFZfJsHwTgA |
| TX_DocumentosLegales | permiso_edificacion_numero | **creado** | fld0MWeaFq3bPyOkn |
| TX_DocumentosLegales | permiso_edificacion_fecha | **creado** | fld3Vg1fQr13GUgCq |
| TX_DocumentosLegales | recepcion_final_numero | **creado** | fldNScUyz00oZ1aq9 |
| TX_DocumentosLegales | recepcion_final_fecha | **creado** | fldn5IBevRZI16Cyf |
| TX_DocumentosLegales | fojas | **creado** | fldzTWo2GtXIFtxWR |
| TX_DocumentosLegales | numero_inscripcion | **creado** | fldCcr705pwKY1L9z |
| TX_DocumentosLegales | ano_inscripcion (number,0) | **creado** | fld585iijZF3oA5Rd |
| TX_DocumentosLegales | lineas_edificacion (multiline) | **creado** | fldDce5e75VuGCddC |
| TX_DocumentosLegales | certificado_numero | **creado** | fldvOcIJ0WDKFWiLF |

## Conflictos abiertos (requieren decisión — NO se modificó nada)

1. **TX_Solicitudes.tipo_propiedad** — spec pide `singleSelect(nuevo·usado)`; el real es `Link → M_TiposPropiedad`. Colisión de nombre + dominio distinto. El dominio nuevo/usado no existe como campo. Sugerencia previa (§20.1): crear `tipo_propiedad_nuevo_usado`.
2. **TX_Solicitudes.origen_direccion** — opciones reales abreviadas `ficha_cliente·cert_avaluo·cert_numero` vs spec `…·certificado_avaluo·certificado_numero`.
3. **TX_Solicitudes.vendedor_origen_dato** — opción real `cert_avaluo` vs spec `certificado_avaluo` (`correo·ficha` coinciden).
4. **TX_Solicitudes.fecha_asignacion** — tipo real `date`; spec pide `dateTime`.
5. **TX_Unidades.superficie_terreno_m2** — no existe con ese nombre; existe `sup_terreno_m2` (number precision 0). Mismo dominio, nombre/precisión distintos. No se creó para evitar duplicado.

## Fuera de alcance (no tocado, según instrucción)
fecha_visita, fecha_envio_informe, TX_Reprocesos, flag/motivo bloqueo por contacto no logrado,
M_Tasadores.notificar_whatsapp, C_SLA plazos nuevos. UI y Make sin cambios en esta tanda.
