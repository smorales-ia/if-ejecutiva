# ROADMAP paridad informe — Planificación T-ROADMAP-PARIDAD-20260924

> **Tanda:** T-ROADMAP-PARIDAD-20260924 · solo lectura · 5 agentes en paralelo (imprenta, textos IA, datos/motor, alta de cliente, planner)
> **Meta del proyecto:** que la UI del Tasador genere un informe IDÉNTICO al PDF gold master (`docs/_referencias/Informe FRANCISCO VERGARA UNDURRAGA_Met6283.pdf`) para CUALQUIER cliente.
> **Insumo principal (no re-auditado):** `docs/_analisis/PARIDAD_informe_UI_20260924.md` + `.xlsx` (228 elementos · 43% OK · P0-1..P0-4, P1-1..P1-9, P2-1..P2-5).
> **Caso de referencia:** VP-2026-0066 (`recNiwM4s1ibr3sbO`), mapeo de MET-6283.
> Verificaciones nuevas de esta tanda vía MCP (lectura) y repo: fechadas 25-sep-2026, citadas en §6.

---

## § 1 · Resumen ejecutivo

El 43% del informe ya tiene camino digital y el núcleo económico está validado 0,00% contra el oráculo; lo que **no existe** es la imprenta (pipeline PDF, P0-1), los textos redactados (P0-2), la mitad cualitativa de la Hoja 3 (P1-1/P1-2), el motor fuera de Refinanciamiento (P0-3) y el onboarding comercial de clientes (P0-4). El camino: **(1)** construir YA — sin esperar nada de Sergio — el ensamblador JSON canónico del informe + matriz de tags §7.3 + plantilla `.docx` provisional + harness de paridad (Tanda 1), con las tandas cortas de fixes S y saneo documental en paralelo; **(2)** cuando Sergio entregue plantilla oficial + credenciales Dropbox/Carbone, conectar SC09/SC10 y los textos Claude (la imprenta queda reducida a plomería); **(3)** con los PDFs históricos por tipo y los valores comerciales por cliente, clonar el motor v32 por tipo (retirando las 5 reglas legacy que hoy **le ganan** a las v32) y ejecutar el checklist de alta de cliente con MetLife como piloto. Todo lo dependiente de layout vive solo en la plantilla — si la oficial cambia todo, únicamente se descarta la `.docx` provisional.

## § 2 · ROADMAP de tandas

| # | Tanda | Objetivo verificable | Depende de | Necesita de Sergio (input externo) | Riesgo principal | Estado |
|---|---|---|---|---|---|---|
| **1** | **T-INFORME-ENSAMBLADOR** | JSON canónico del informe (ensamblador server-side + Route Handler) + matriz de tags §7.3 (228 E-ids → tag → fuente) + golden VP-2026-0066 + harness con **score de paridad** + plantilla `.docx` provisional + validador anti-cifras de textos IA con fixtures del gold master | — | **Nada** | La plantilla oficial podría cambiar el layout → solo invalida la `.docx` provisional, no la matriz ni el ensamblador | **EJECUTABLE ★** |
| 2 | T-FIX-S | P1-3: cron UF escribe también `tipo_cambio_usd` (el campo ya existe, `fldlFxWDnK67OajaE`) + alerta a 2 fallos + borrar fila huérfana `recrQdCDkJvGgxClK` · P1-6a: `uso_campo_destino` `sup_m2`→`sup_construccion_m2` · P1-8: promedio de muestra al motor (lector TX_Comparables patrón `sumCuadroValoracion` + `F_UFm2_promedio` ya existente linkeada a la regla + 2 columnas) y fila TASACION cableada (F-2) | — | Nada (P1-6b —redeclarar año/superficies/material a TX_DatosTasacion— espera **decisión A4**; se saca de alcance si no llega) | Deploy de scripts de automation es paste manual en UI Airtable (MCP no edita `customScript`) — el cierre e2e queda condicionado | **EJECUTABLE** |
| 3 | T-SANEO-DOC | P2-5: CLAUDE.md refleja realidad (E1/E2/E3 NO activos; SC05 existe inactivo), C_AutomationsAirtable realineada, `VP-NaN-0066` y CI-024 saneados, SC-SLA-Envio registrado en Z_EscenariosMake | — | Nada | Bajo; cuidar la regla sólo-append de aprendizajes.md | **EJECUTABLE** |
| 4 | T-UI-HOJA3-COLUMNAS | P1-1: ~35-40 columnas en 5 tablas existentes para lo que la UI captura y descarta (incl. los 7 silenciosos de E.6 Detalles) + PATCH ampliado · P1-2: ~30-35 columnas + 1-2 sub-secciones UI + **rescate de `M_Zonificacion`** (ya existe: 1.636 filas, choices sucias) · columnas destino de textos IA (`sintesis_propiedad_texto`, `descripcion_sector_texto` en TX_DatosTasacion) | T1 (matriz de tags: crear columnas UNA vez y con destino cierto — PARIDAD §6) | Nada duro (schema Airtable = autorización de tanda). Sub-bloque catálogo normativo: **validar con Héctor** vigencia del seed de zonas | Volumen (~80 campos): partir en 4a columnas / 4b UI si excede una sesión | **EJECUTABLE** (tras T1) |
| 5 | T-PDF-PIPELINE | P0-1+P0-2 conectados: SC09 (orquestador delgado: GET ensamblado → Claude textos RN-25/RF-32 → Carbone) + SC10 (Dropbox + versionado RN-56) activos; primer PDF automático de VP-2026-0066 | T1 (consume ensamblador+matriz+plantilla); paridad completa requiere además T4/T6/T7 | Plantilla `.docx` oficial **o** ratificación de la provisional · **credencial Dropbox válida** (la actual es un access token `sl.` expirado) · cuenta/API key **Carbone** · aprobación de escenarios nuevos en Make | SC09/SC10 (ids 5748459/5750023/5791413) están "Pendiente" sin blueprint: es reconstrucción, no reactivación | **TRABADA** |
| 6 | T-MOTOR-TIPOS | P0-3: set canónico de fórmulas cerrado (incl. `F_VidaUtil` ya existente) → reglas v32 clonadas por `tipo_informe` (Mutuo Hipotecario primero: 7 solicitudes hoy caen al wildcard) → **retiro/migración de las 5 reglas legacy prioridad-200 que hoy ganan a las v32** → regresión 13/13 por golden master | T2 (set completo con promedios), T1 (harness) | **PDFs históricos por tipo** (hoy solo existe el de Refinanciamiento) + **valores comerciales por cliente** (los factores viven embebidos en las reglas legacy; migrarlos a M_Clientes/C_Factores) | Validar reglas sin oráculo = cifras plausibles incomprobables; y desactivar legacy sin migrar factores rompe clientes activos | **TRABADA** |
| 7 | T-ONBOARDING-CLIENTES | P0-4+P1-5+P1-7+P2-4: checklist formal de 12 ítems (ver A4, formaliza RF-34) ejecutado con **MetLife como piloto**; saneo de ~18 clientes duplicados y fila basura; factores para los 12 clientes sin ellos; C_VariablesCliente real (logo/revisor); C_SLA por par; `uf_m2_*` en M_Comunas según cobertura | — (guards H5/H6/H7 ya publicados) | **Valores comerciales por cliente**, logos y nombre_revisor, plazos SLA contractuales, cobertura de comunas, decisión de fila canónica por duplicado | Poblar con valores inventados **silencia los guards**: hoy fallan ruidoso, que es lo correcto | **TRABADA** |
| 8 | T-MAPA-REFERENCIAS | P1-4: Google Static Maps desde lat/long + geocoding + foto adjunta por comparable | T4 recomendable | **API key Google** (Static Maps + Geocoding) y decisión de costo | Cuota/costo; fallback si comparable sin lat/long | **TRABADA** (sub-bloque foto-por-comparable ejecutable) |
| 9 | T-CIERRE-CUALITATIVO | P1-9+P2-1/2/3: firma en M_Tasadores, fecha visado (alcance IF-04), factores remate/liquidación 0,65/0,825 vs `velocidad_venta_estimada`, categorías de fotos faltantes | T5 | **Imágenes de firma** por tasador · decisión 0,65/0,825 · decisión alcance IF-04 | Decisiones dispersas: agrupar en una sola sesión de decisiones | **TRABADA** |

**Secuencia recomendada:** T1 → (T2 ∥ T3) → T4 → T5 (al llegar inputs) → T6 → T7 → T8/T9. T2 y T3 son cortas e intercalables.

**Riesgos transversales:**
- **Reglas legacy ganan a v32** (hallazgo nuevo, §6): AT01 ordena `score desc, prioridad desc`; las 5 legacy (prioridad 200, score 3, 19 fórmulas v3.1 no validadas) derrotan a las v32 (score 2). La decisión vigente de VP-2026-0066 la ganó `Regla_MetLife_Refinanciamiento_Casa`, no `REGLA_REFI_CASA_V32`. El camino validado 0,00% **no corre** cuando el cliente matchea una legacy.
- **Deploy manual inevitable**: ningún script de Airtable Automation se despliega por API/MCP — toda tanda que toque AT01/AT03/CRON planifica el paste UI + verificación como bloque propio.
- **Documentación stale activa**: CLAUDE.md aún dice "E1/E2/E3 ✅ ACTIVO — no tocar" (falso) — hasta T3, un agente nuevo puede auto-bloquearse o duplicar escenarios.
- **Guard `origen_dato=tipeado`**: RF-09 es no-op sobre casos pre-poblados — harness y regresiones deben usar 0066 para valores tipeados-oráculo y 0067 para flujo RF-09.
- **Separación datos/plantilla**: todo lo dependiente de layout vive solo en la `.docx`; meter layout en código o columnas propaga el retrabajo si cambia la plantilla oficial.

## § 3 · Tanda 1 en detalle EJECUTABLE — T-INFORME-ENSAMBLADOR

**Elección justificada** (candidatas evaluadas: fixes S · columnas P1-1 · saneo documental · ensamblador): los fixes S pulen el 43% que ya funciona y su cierre e2e depende de pastes manuales; crear columnas antes de la matriz de tags contradice la recomendación explícita de la auditoría ("crear columnas una sola vez y con destino cierto"); el saneo es higiene que no acerca ningún elemento a la paridad. El ensamblador es el único candidato que ataca un P0 sin esperar nada de Sergio, produce el artefacto que destraba T4, deja T5 reducida a "conectar Make + credenciales", e instala una métrica de paridad regresionable. Su riesgo de trabajo tirado está acotado por diseño: matriz, ensamblador y golden son independientes del layout.

**Enfoque de imprenta que materializa (A1):** híbrido — Carbone como destino final (lo manda la spec: §7.1, RF-30/31/32/33/39, RNF-05, SP-05), pero **el cerebro en el repo**: ensamblador y generador de textos como módulos TypeScript testeables con vitest; SC09 queda como orquestador delgado de 5-6 módulos (trigger `estado=calculada` → GET ensamblado → Carbone → Dropbox → fila TX_DocumentosGenerados → `pdf_listo`).

- **Objetivo:** que exista una única fuente programática del informe: JSON canónico por solicitud cubriendo los 228 E-ids (con `null` explícito y `huecos[]` trazados a su P-id), su matriz de tags, un golden de VP-2026-0066 y un harness que fija el score de paridad (baseline ~43%) como métrica que toda tanda futura debe subir. Más una plantilla `.docx` provisional apta para probar render en T5.
- **Autorización:** solo escrituras de código/artefactos en el repo (rama nueva). **Cero writes a Airtable/Make.** Lecturas runtime vía Route Handler con `AIRTABLE_TOKEN` (patrón existente); tests con mocks (prohibido escribir a la base productiva desde tests). Commit/push los hace Sergio.
- **Bloques en orden:**
  1. **Matriz de tags v1** (`docs/_artefactos/plantillas/matriz-tags-informe.md`): derivada del Excel gemelo — por tag: nombre `{d.xxx}`, E-ids cubiertos, fuente real (tabla.campo con FIELD_ID / motor / IA / hardcode-plantilla), estado OK·HUECO·MetLife-only y P-id. Es el "artefacto vivo §7.3" hoy inexistente. Las 12 familias de placeholders (identificación, partes+firmas, propiedad, SII con loop, cuadro con loop, terminales/monedas, comparables con loop+imagen, rentabilidad, textos IA, cualitativa Hoja 3, recintos ×3 loops, imágenes/condicionales) están esbozadas en el informe de A1.
  2. **Ensamblador** (`lib/informe/ensamblador.ts`, server-only): extiende el modelo canónico de `lib/tasador/lectura-informe.ts` (ya lee 8 tablas y arma los bloques 1-8); suma TX_Calculos/terminales, rentabilidad, y los promedios de comparables hoy efímeros como campos calculados (puente hasta que T2 los persista); emite `InformeContexto` completo con `huecos[]`.
  3. **Route Handler** `app/api/tasaciones/[id]/informe-data/route.ts` (GET): expone el JSON; será el payload de SC09 en T5.
  4. **Golden + harness Etapa A** (`lib/informe/golden-met6283.ts` + `ensamblador.test.ts`, patrón `vi.mock` + `llamar()` del repo): compara tag a tag contra los valores del PDF/Excel gemelo, tolerancia 0 en numéricos, HUECOs asertados como `null` explícito, **score de paridad asertado**. Diseño de Etapa B (render: extracción pymupdf + normalización es-CL + conteo de imágenes + hash reproducible RF-39) queda documentado para T5.
  5. **Plantilla provisional** (`docs/_artefactos/plantillas/Informe_VProperty_provisional_v0.docx` + nota de alcance): estructura de las 8 hojas con los tags de la matriz; fidelidad estructural, no pixel-perfect; marcada PROVISIONAL y descartable. Fallback si el tooling local no genera el binario: esqueleto XML/markdown declarado (no simulado).
  6. **Textos IA adelantados sin writes** (A2): prompt v1 de síntesis/sector con los párrafos del gold master como patrón + **validador anti-cifras** como módulo TS puro en `lib/` (extrae números con formato es-CL y los casa contra el payload; discrepancia → rechazo, RF-32 literal) con los párrafos transcritos como fixtures + draft del blueprint `SC-Textos` (clon del patrón SC-RF09, modelo `claude-sonnet-4-6`) versionado en `docs/_artefactos/make/`.
  7. **Cierre:** `pnpm test`/`typecheck`/`build` limpios; doc de cierre en `docs/_analisis/`; entrada en aprendizajes.md solo si hubo inconvenientes.
- **Verificación de cierre:** (1) `GET /api/tasaciones/[id]/informe-data` devuelve el JSON completo para VP-2026-0066 en dev; (2) harness verde con score reportado y cada HUECO trazado a su P-id; (3) la matriz cuadra contra los 228 E-ids (suma = 228, sin duplicados); (4) validador anti-cifras verde sobre fixtures; (5) build limpio; (6) cero writes a Airtable/Make en toda la tanda.

## § 4 · Inputs que debe entregar Sergio

| Input | Para qué | Tanda que destraba |
|---|---|---|
| **Plantilla `.docx` oficial** corporativa (o decisión: ratificar la provisional de T1) — y resolver la dualidad `MUTUO_MET.docx` (AT01) vs `Plantilla_Base_METLIFE` (seed) | Render final con el formato real de la empresa; hoy 0/16 filas de C_Plantillas tienen archivo | T5 |
| **Credencial Dropbox válida** (refresh token OAuth real; el actual es access token `sl.` expirado) | SC10 almacena el PDF; también desbloquea uploads/RF-09 e2e | T5 |
| **Cuenta/API key Carbone** en Make (SP-05 presupone plan 10.000 docs/mes) | Render `.docx`→PDF | T5 |
| **PDFs históricos por tipo de informe** (Mutuo Hipotecario, Leasing, Comercial…; hoy solo existe el de Refinanciamiento MET-6283) | Golden masters de regresión para clonar v32 por tipo | T6 |
| **Valores comerciales por cliente**: `tasa_cap_rate`, `factor_seguro`, `factor_garantia` para los 12 clientes sin factores (incl. la fila MetLife duplicada) + decisión de fila canónica por cada uno de los ~18 duplicados | Que H5/H6 dejen de abortar con datos REALES (no inventados) y que retirar las reglas legacy no rompa clientes | T6 · T7 |
| **Logos (archivo/URL) y `nombre_revisor` por cliente** → C_VariablesCliente (hoy 0 valores reales; el placeholder MetLife está vacío) | Portada correcta — la de MET salió con marca Austral Leasing (H9) | T7 |
| **Plazos SLA contractuales** por par cliente × tipo (hoy: default global 3/2/3 + 1 fila MetLife incompleta) | Semáforo RN-04/AT08 con umbrales reales | T7 |
| **Cobertura de comunas**: estudio `uf_m2_*` para las 297 comunas sin datos, o decisión explícita de zona de operación | Guard H7 deja de abortar el 83% del país | T7 |
| **Decisión A4** (gate doble-escritor RF-09→TX_DatosTasacion) | Que año/superficies/material extraídos lleguen al motor | T2 (sub-bloque b) |
| **Decisión E-85**: "análisis de las referencias" como texto fijo de plantilla (recomendado) vs párrafo IA por caso | Cierra el alcance de textos de T5 | T5 |
| **Decisión factores 0,65/0,825**: conectar a `velocidad_venta_estimada` o ratificar fijos | Coherencia remate/liquidación | T9 |
| **API key Google** Static Maps + Geocoding | Mapa de referencias con pines | T8 |
| **Imágenes de firma** por tasador + decisión alcance IF-04 (fecha visado) | Bloque de firmas completo | T9 |
| Validación de **Héctor**: vigencia del seed de M_Zonificacion (1.636 filas, 04-jun) y frontera catálogo-vs-caso de la Hoja 3 | Catálogo normativo confiable para E-125..157 | T4 (sub-bloque) |
| *(No input, sí deploy manual)*: pastes de scripts AT01/AT03/CRON en UI Airtable + activación SC-SLA-Envio | Cierre e2e de T2 y del despacho SLA | T2 |

## § 5 · Recomendación consolidada del equipo

Arrancar **hoy** con T-INFORME-ENSAMBLADOR: es la única tanda P0 que no espera nada de Sergio, convierte la auditoría de 228 elementos en artefactos ejecutables (matriz de tags, JSON canónico, score de paridad regresionable) y deja la imprenta reducida a plomería el día que lleguen plantilla y credenciales; intercalar T-FIX-S y T-SANEO-DOC como tandas cortas mientras tanto. En paralelo, Sergio junta los tres paquetes de inputs por orden de impacto: **(1)** plantilla oficial + Dropbox + Carbone (destraba el primer PDF automático), **(2)** PDFs históricos por tipo + valores comerciales por cliente (destraba motor multi-tipo y el retiro de las reglas legacy — que hoy son el riesgo silencioso más serio: el camino validado 0,00% no es el que corre en producción para MetLife), **(3)** logos/revisor/SLA/comunas (destraba el onboarding con MetLife como piloto). Con T1+T5 hay informe automático de MetLife; con T6+T7, de cualquier cliente; T8/T9 cierran la paridad visual fina.

## § 6 · Hallazgos nuevos de esta tanda y evidencias clave

Hallazgos que NO estaban en la auditoría de paridad (todos verificados en modo lectura, 25-sep-2026):

1. **Las reglas legacy ganan a las v32.** AT01 ordena `score desc, prioridad desc` (`AT01-ResolverMotorReglas.js:438`); las 5 legacy cliente-específicas (prioridad 200, score 3, 19 fórmulas v3.1) derrotan a las v32 (score 2). Evidencia en producción: `A_DecisionesMotor` `rec8lxTwMAqXyncex` (VP-2026-0066, 17-sep) ganada por `Regla_MetLife_Refinanciamiento_Casa`; Mutuo Hipotecario cae al wildcard (`recOjkCK0c8uQ6QlA`, `recePFE8X0TndMiJd`). SUPUESTO: la validación 0,00% de T-MC-P0 corrió por la ruta v32 con contexto forzado.
2. **M_Zonificacion ya existe y cubre la Hoja 3**: 1.636 filas (seed 04-jun-2026, formato `ComunaZona`), Link a TX_DatosTasacion (`fldkOAyWhqx2jC8Cr`); sucia (choices basura, "Urbano"/"Urbana" duplicado). P1-2 es rescate, no creación.
3. **El self-healing del cron UF funcionó**: el fallo del 24-sep (`recJKeeofeA9r7jhL`) fue reparado por LOOKBACK_DIAS=7 en la corrida del 25 (`rec4dFi3w8qMeZBOY`); `H_PreciosUF.tipo_cambio_usd` ya existe (`fldlFxWDnK67OajaE`) — P1-3 es extender, no crear.
4. **Los 7 campos silenciosos son la sub-sección E.6 Detalles completa** (`seccion-edificacion.tsx:302-318`): ni en `CAMPOS_SIN_DESTINO` (`lib/tasador/validators/index.ts:335-363`) ni en `noPersistidos[]`.
5. **MetLife está triplicado** (`recIg8NtVhptXkEUJ` canónico · `recimE810cuyWIR9q` SIN factores — H5/H6 latente · `recwxQlPhJTXgig93`); M_Clientes: 92 filas, ~18 clientes duplicados, 1 fila basura (`recYGkxnFlATx7Nv5`), 0/92 con Logo attachment; C_Factores tiene 6 filas MetLife (remate 0,65, liquidación 0,55, USD 950…) vinculadas solo al canónico.
6. **`F_VidaUtil` y `F_UFm2_promedio` ya existen** en C_Formulas (`rec52S28qR4kBR7MU`, `recFcpOeKjXNunBlj`, v3.1 activas) — P1-9/P1-8 es linkearlas a la regla, no crearlas.
7. **Los textos IA no tienen campo destino en ninguna tabla** (grep negativo en schema y código) y el schema RF-32 (`{sintesis, programa, descripcion_sector}`) **no incluye** E-85 ni E-57: E-85 conviene como texto fijo de plantilla y E-57 es plantilla con 2 variables — pero depende de la columna `afecto_expropiacion` (P1-1).
8. **No hay cliente Anthropic en el código Next.js** (`app/api/extraccion/` no existe pese a listarlo CLAUDE.md): el camino natural de textos es Make, clonando el patrón SC-RF09 (módulo 10: `claude-sonnet-4-6`, contrato JSON estricto probado en producción).
9. **pymupdf y openpyxl disponibles en este WSL** y el gold master tiene texto extraíble → el harness de paridad (Etapas A y B) es automatizable sin servicios externos. SUPUESTO por validar: render local con npm `carbone` sin cuenta.

Evidencias detalladas (archivo:línea y record ids) en los informes de los 5 agentes, citadas a lo largo de §2-§4. Spec: §7 completa (líneas 4845-5001), RF-32 (4927-4944), RF-34 (4572-4583), Origen v1.7 (205-210, 1010-1030, 1164-1171).

---
*Planificación de solo lectura — cero writes en Airtable/Make/código. Único archivo nuevo: este documento. Los huecos provienen de la auditoría T-INFORME-PARIDAD-20260924; esta tanda solo los ordenó y verificó lo citado en §6.*
