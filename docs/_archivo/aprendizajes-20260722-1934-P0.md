# Aprendizajes P0 — Inventario y alineación con lo existente

- **Fecha:** 2026-07-22
- **Hora inicio → fin:** 19:30 → 19:40
- **Duración:** ~10 minutos
- **Modo Claude Code usado:** auto mode on (contrato 🟢 libre)
- **Commit asociado:** (Sergio lo agrega tras commit)

## Resumen ejecutivo
- Se barrió el repo v0.dev con `git ls-files` + `grep` y se generó `docs/_notas/inventario-if02.md` con las 7 secciones + overrides P1–P9 + checklist de riesgos.
- El repo está **mucho más avanzado** que lo que asume el plan: wizard, formulario, lista, detalle, diálogo de asignación y sheet de documentos ya existen.
- No se modificó ningún archivo de código (solo 2 docs generados).

## Decisiones técnicas
- Los tipos de dominio viven en `lib/console-data.ts` (no en `lib/types/`) → P1 amplía ahí en vez de crear carpeta nueva.
- Convención camelCase del repo (`contactosVisita`, `tipoPropiedadNuevoUsado`, `slaDias`) se declara ganadora sobre el snake_case del plan §2.1, para no romper list/detail/form/validators.
- Se mantiene el patrón master-detail (`console-shell.tsx`, estado `selectedId`) en vez de introducir navegación por ruta `/solicitudes/[id]` que asumían P5/P6.

## Overrides aplicados (rutas reales vs plan)
- Todas las carpetas propuestas por el plan (`wizard-nueva-solicitud/`, `form-solicitud/`, `panel-lista/`, `detalle-solicitud/`, `dialogo-asignacion/`, `sheet-documentos/`) ya existen como archivos únicos → cada P extiende el archivo real, no crea carpeta.
- `lib/airtable/client.ts` y `lib/make/webhook.ts` del plan → ya existen como `lib/airtable-client.ts` y `lib/make-client.ts`.
- `POST /api/solicitudes` del plan → hoy es `POST /api/webhooks/crear-solicitud`.

## Bugs / obstáculos y resolución
- `tree` no está instalado en el entorno WSL → se usó `git ls-files` + `find` como fallback para el árbol. Sin impacto.

## Deuda técnica para P siguientes
- **Conflicto REGLA A ↔ repo:** existe `reasignar-tasador-dialog.tsx` + `MOTIVOS_REASIGNACION` + vista `reasignar`, que la Regla A dice que no deben existir → decisión pendiente de Sergio (P5/P6/P7).
- Detalle usa estado mock en memoria; no persiste a Airtable → cablear rutas reales en P6/P7/P9.
- `Adjunto` duplicado en `console-data.ts` y `adjuntos.ts` → unificar en P1.
- Enum `Vista` del repo (`activas|sla_riesgo|reasignar|pausadas|aprobadas|cartera`) ≠ vistas del plan → reconciliar en P5.
- Faltan deps para P5: `swr`, `react-day-picker`/`date-fns`, `use-debounce`, y shadcn `calendar`/`date-range-picker`.
- REGLA B: el toast de errores existe, pero falta confirmar/crear el Alert destructivo persistente al inicio del form → P4.

## Reglas nuevas → MIGRAR a docs/aprendizajes.md
- E-P0-1: Antes de crear cualquier archivo, consultar la sección "Overrides al plan" del inventario; el repo v0 ya implementó casi toda la estructura como archivos únicos, no como las carpetas del plan.
- E-P0-2: La convención de nombres de dominio es camelCase (repo), no snake_case (plan). Toda extensión de tipos respeta camelCase.
