# Aprendizajes P8 — Sheet Documentos y Adjuntos

- **Fecha:** 2026-07-22
- **Hora inicio → fin:** 22:25 → 22:32
- **Duración:** ~7 minutos
- **Modo Claude Code usado:** accept edits on (contrato 🟡 pausa-en-comandos)
- **Commit asociado:** (Sergio lo agrega tras commit)

## Resumen ejecutivo
- El sheet (`documentos-adjuntos-sheet.tsx` + `document-checklist.tsx`) ya estaba casi completo: checklist de 15 tipos, `FileUploadZone`, modo consulta con Ver/Descargar, confirmación al desmarcar.
- P8 fue verificación §9.3 + un fix de wiring RN-59 (modo consulta).
- `pnpm tsc --noEmit` limpio (exit 0).

## Verificaciones §9.3 (cumplían, sin cambios)
- Checklist de **15 tipos** desde `TIPOS_DOCUMENTO` (`checklistInicial`).
- Subir por fila del checklist con preview (client-side); zona de "Adjuntos libres" con `FileUploadZone`.
- Desmarcar un documento con archivo → `AlertDialog` "¿Quitar este documento?".
- Modo consulta: `ChecklistConsulta` + `AdjuntosConsulta` (solo Ver/Descargar, sin subir/eliminar).
- Cerrar el Sheet no pierde archivos (el estado se reinicia por `solicitud.id`, no por cierre).

## Fix aplicado (RN-59)
- El sheet recomputaba `soloLectura = solicitud.estado !== "creada"`, **ignorando el tasador** — divergía de RN-59 (`estado ≠ creada Y tasador asignado`). Se agregó la prop `readOnly?: boolean`; el sheet la respeta (`readOnly ?? estado !== "creada"` como fallback) y el detalle ahora pasa `readOnly={soloLectura}` (el `soloLectura` correcto de RN-59, línea 140 de `solicitud-detail.tsx`). §9.2.7.

## Overrides / notas
- Se mantuvo el sheet como archivo único (no la carpeta `sheet-documentos/` del plan).
- El wording del `AlertDialog` de desmarcar ("¿Quitar este documento?") no es un literal §6, así que su redacción es libre.

## Deuda técnica para P siguientes
- La subida real de adjuntos desde el detalle (con `solicitud_id` + `tipo_documento` a `/api/adjuntos/upload`) tiene la misma tensión mock/real que P6/P7: el detalle es mock en memoria y el endpoint exige record id + Make. → P9.
- Los archivos del checklist/adjuntos libres viven en estado local del sheet (no persisten a Airtable todavía) → P9.

## Reglas nuevas → MIGRAR a docs/aprendizajes.md
- E-P8-1: El modo consulta (RN-59) se calcula **una sola vez** en el detalle (`estado ≠ creada Y tasador`) y se **pasa como prop** (`readOnly`) a los hijos (sheet de documentos, bloques). Un hijo no debe recomputarlo con una condición más débil (solo estado) — eso genera divergencias de bloqueo. Fallback por estado permitido para compatibilidad, pero el valor autoritativo viene del padre.
